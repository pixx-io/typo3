# cat=basic; type=int; label=File Storage ID (Default is 1 = fileadmin in standard TYPO3 installations)
filestorage_id = 1

# cat=basic; type=string; label=Default subfolder (of choosen File Storage, e.g. "images")
subfolder =

# cat=basic; type=string; label=Allowed download formats: Control which download formats are available in the plugin SDK iframe. Use comma-separated values for multiple formats (e.g. "jpg,png,pdf"). Valid formats: original, preview, jpg, png, pdf, tiff. Leave empty for all formats.
allowed_download_formats =

# cat=basic; type=boolean; label=Use CDN Links: The system won't create local copy of the file, but will use always up-to-date file from pixx.io. A plan with CDN Links is required for that.
use_cdn_links = false

# cat=metadata; type=string; label=Metadata for alternative Text (default = Alt Text (Accessibility))
alt_text = Alt Text (Accessibility)

# cat=sync; type=string; label=pixx.io URL (domain.px.media)
url =

# cat=sync; type=string; label=pixx.io user name or email: This value is not needed. You can use it to remember which user the refresh token belongs to.
user_id =

# cat=sync; type=string; label=pixx.io refresh token: This value is used only for automatic sync.
token_refresh =

# cat=sync; type=boolean; label=Delete file, if the file does not exist in the mediaspace
delete = false

# cat=sync; type=boolean; label=Replace the file if a new version is available in pixx.io or if the file has been replaced. (Only available when CDN Links are disabled.)
update = false

# cat=sync; type=boolean; label=Update the metadata when new metadata is available in pixx.io.
update_metadata = false

# cat=sync; type=boolean; label=Auto-login with the sync access data in the image picker.
auto_login = false

# cat=sync; type=int [1-500]; label=The amount of files that should be synchronized through a single run
limit = 20

# cat=proxy; type=boolean; label=Use proxy configuration
use_proxy = false

# cat=proxy; type=string; label=Provide a proxy URL that contains a scheme like this: http(s)://username:password@host:port (optional)
proxy_connection =